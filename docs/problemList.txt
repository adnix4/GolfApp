add auction items and bid data to test data 
The data from the mobile app scorer should feed into the admin score card with the admin score card to be only for adjustments, monitoring, and adding in scores if there is no service for mobile devices. Web leader board should also be based off the same information.
Add strokes back, best hole, and best hole score columns to the web leader board.
Golfers should see admin corrections to their OWN scores on the mobile scorecard. Today the mobile scorecard renders only from local device state (pending_scores in SQLite) and sync is push-only — admin edits to the scores table never flow back to the device. The mobile LEADERBOARD already reflects admin changes (SignalR /hubs/tournament + 15s poll), but the golfer's own hole-by-hole scorecard does not. Fix: add a server-to-device score pull/merge so admin adjustments (and resolved conflicts) update the golfer's local scorecard — e.g. broadcast the corrected score over the tournament hub to the team's device and/or refetch the team scorecard on poll, reconciling against unsynced local edits.