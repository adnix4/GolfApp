# Problem List

## Priority order (set 2026-08-01)
Ranked across every OPEN item in this file. Numbers are chronological and are
referenced by git history and by other entries, so nothing is renumbered —
this section is the running order, the entries below stay where they are.
Tiers are ordered by what breaks a real event soonest; work top-down.

P0 — BREAKS A LIVE EVENT, SMALL FIX, DO FIRST
     [x] D17  client + server halves both fixed 2026-08-01
     [x] D15  session poll split 2026-08-01
     [x] D14  pledges now summed 2026-08-01
     [x] D16  fee UI hidden on free events 2026-08-01
     [x] U1   strokes sum + Hole Complete / Edit Score 2026-08-01
     This tier is now clear.

P1 — PILOT PATH (external lead times first; D1 gates D3 and D10)
     D1   accounts / domain / Apple / Stripe / email — start now, rest waits
     [x] D2  already complete — verified 2026-08-01, nothing to write
     D3   staging environment
     D4   dress rehearsal — the pilot succeeds or fails here
     D5   mobile distribution (EAS / TestFlight) — now carries two verifications
          nothing else can do: SDK 57 on real hardware, and the outbid push
          end-to-end. D4 needs a build from here, so in practice D5 gates it.
     D6   pilot ops — backups, error monitoring, day-of runbook

P2 — CORRECTNESS THE ORGANIZER WILL HIT ON THE DAY  (tier clear)
     [x] U5  auto-assigned on the Active transition 2026-08-01
     [x] D11 per-golfer fee endpoint at EventStaff 2026-08-01

P3 — PERFORMANCE BEFORE A REAL CROWD
     [x] D18  both queries split 2026-08-01

P4 — FEATURES / POLISH
     [x] U4  proxy bidding, Silent items only, steps by the item's increment 2026-08-01
     [x] U2  }  one combined marquee (sponsors + open lots), normal AND
     [x] U3  }  TV mode, on the existing socket 2026-08-01
     U6   evaluate buy-it-now — what triggers it, what it bills, and whether it
          belongs on a proxy lot at all. Decision needed, not a coded fix.
     U7   follow-ups from the 2026-08-02/03 work (sponsors, ticker, TV branding,
          bid confirmation). (a) was the security item and is FIXED 2026-08-15 —
          it was pulled forward out of this tier, as the note said to. (b)-(m)
          remain, and are decisions or polish rather than defects.

P5 — BLOCKED OR DEFERRED (no action available yet)
     D19  dev-loop fixes landed; three follow-ups, one gated on D6
     D7   app-store links — blocked until the listings exist
     D10  card-on-file at check-in — blocked on D1 Stripe activation
     T4   thin-service test coverage — low ROI, deliberate defer

## Standing rules (extracted 2026-08-15)
Completed entries below were compressed to one line each. These are the durable
decisions that came out of them — the parts that govern code not yet written.
Breaking one of these re-opens a bug that was already paid for. Each names the
entry it came from; the full reasoning is in git history for that entry.

- ENTRY-FEE STATE LIVES ON THE GOLFER (D13). Never reintroduce a team-level
  paid boolean as a source of truth. teams.entry_fee_paid is still written for
  continuity and read nowhere.
- USE notify(), NOT Alert (D12). react-native-web ships Alert as a literal
  no-op, so every Alert.alert on the web build goes nowhere. Anything that
  reports to a golfer goes through src/lib/notify.ts.
- PROXY BIDDING IS SILENT ITEMS ONLY (U4). Do not widen it. A Live lot is
  called aloud by an auctioneer and Fund-a-Need pledges stack rather than
  compete. The proxy step is the item's bid_increment_cents, not a flat $1.
- STARTING HOLES ARE ASSIGNED ON StartType == Shotgun ONLY (U5), never by
  Format == Scramble. A scramble on tee times starts everyone at hole 1;
  stamping holes there makes the scorecard lie. Settled — do not widen.
- CHECK-IN IS ALLOWED IN Active OR Scoring (D9), so late arrivals stay
  eligible to bid under the D8 waiver. Completed and Cancelled stay closed.
- A CARDLESS WINNER CANNOT BE CHARGED (D8). The charge fails, the winner is
  marked ChargeStatus.Failed, and the item still closes. The organizer settles
  by hand. Confirm they will chase payment before enabling this for a real event.
- CLIENT AND SERVER BID RULES MOVE TOGETHER (D12, U4, U7m).
  packages/shared-types/src/auctionRules.ts mirrors AuctionBidRules; the
  lockstep comment at its head names the server file to change with it.
- A ROTATED REFRESH TOKEN IS REPLAYABLE FOR 30s (D17), knowingly traded to stop
  concurrent-refresh logouts. Reuse outside the window logs "Possible token
  theft" — wire that into D6's monitoring.
- /health DOES NOT PROBE DB OR REDIS (D2). It answers "the process is up", not
  "the API can serve". If D6 wants dependency health, that is a new task.
- TESTING THE PRODUCTION STARTUP GUARDS: `dotnet run` applies
  Properties/launchSettings.json, which pins ASPNETCORE_ENVIRONMENT=Development
  and silently masks them (D2). Use --no-launch-profile.

## Deployment path — real-world pilot (2026-07-09)
Goal: deploy Golf Fundraiser Pro for a real-world test event. Ordered phases;
mark [x] as completed. D1 items have external lead times — start them first,
they run in parallel with everything else. Completed entries are one-liners;
their standing decisions are in the section above.

[ ] D1. DECISIONS & ACCOUNTS (user actions; longest lead times)
        - Hosting choice. Spec default: API on Railway (any container host
          works), web on Vercel, admin as a static expo export served
          anywhere, managed Postgres+PostGIS and Redis. A single VPS with
          docker-compose + reverse proxy is also fine at pilot scale.
        - Domain + DNS (.env.example assumes golffundraiser.pro with api. and
          app. subdomains).
        - Apple Developer Program enrollment ($99/yr; TestFlight review adds
          days). SDK 57 constraint: shipped iOS builds require iOS 16.4+
          (Xcode 26.4 — EAS cloud runners handle it, Windows dev unaffected,
          but older devices are dropped). Confirm EAS image + minimum-OS
          commitment before releasing a build.
          The SDK 56 → 57 upgrade (2026-08-15) did NOT move this floor —
          ExpoModulesCore.podspec still declares :ios => '16.4' — so the
          device-support commitment is unchanged from what was scoped here.
        - Stripe live account activation (business verification lead time) —
          or run the pilot in test mode and use the cash mark-paid-at-check-in
          path for real fees.
        - Email provider (SendGrid or similar) + SPF/DKIM domain auth.
          CRITICAL PATH: join email-verification codes must actually arrive
          once the dev bypass code is off (see A3 history in git).

[x] D2. CODE HARDENING — every bullet was already shipped; verified per-bullet 2026-08-01, nothing written.

[ ] D3. STAGING ENVIRONMENT (after D1 hosting/domain decided)
        - Provision managed Postgres WITH PostGIS (plain postgres breaks GPS
          features — see infra/docker-compose.yml warning), Redis, and object
          storage for uploads (IFileStorage S3/R2 support is merged and
          MinIO-verified; local-disk uploads don't survive redeploys).
        - Deploy API + web + admin static export; set NEXT_PUBLIC_* /
          EXPO_PUBLIC_* build-time URLs; register the Stripe webhook endpoint
          (fail-closed outside Development is already in); TLS everywhere.
        THE API IMAGE PIPELINE NOW WORKS (2026-08-15). deploy-api.yml had failed
        at workflow VALIDATION on all 40 runs since 2026-05-23 — `secrets` is not
        available to a step-level `if`, which rejects the whole file before any
        job starts, so zero jobs ever ran and the API had never once been built
        by CI. Fixed in PR #41; it now builds and pushes
        ghcr.io/adnix4/golfapp/api on every apps/api change. RAILWAY_TOKEN and
        RAILWAY_SERVICE_ID are still unset, so it skips the deploy call and
        reports success — setting them (a D1 decision) is the last step, and the
        image is deployable by hand meanwhile. Note only EXPO_TOKEN is
        configured today; TURBO_TOKEN/TURBO_TEAM are optional cache credentials.

[ ] D4. DRESS REHEARSAL on staging (the pilot succeeds or fails here)
        - /seed-demo-event full lifecycle against staging.
        - Score from real phones, including airplane-mode offline entry and
          sync-back on reconnect.
        - Verification emails arriving on real inboxes (Gmail/Outlook).
        - Stripe test charge + webhook end-to-end; cash mark-paid path.
        - If D8 shipped: bid from a phone with no saved card after check-in, then
          walk the failed-charge resolution in admin.
        - TV leaderboard mode on an actual screen.

[ ] D5. MOBILE DISTRIBUTION
        - EAS builds: iOS via TestFlight, Android via internal testing track
          (or direct APK for a friendly pilot). Expo Go is NOT viable for a
          real event — golfers need an installable build.
        - Push notification credentials (EAS project) verified end-to-end.
        - Wire up the store links once listings exist — see D7.
        NOW ALSO THE ONLY WAY TO VALIDATE SDK 57 (raised 2026-08-15). Both Expo
        apps moved SDK 56 → 57 (react-native 0.85.3 → 0.86.2) and merged on
        type-check, vitest and the admin WEB export alone. There is no eas.json
        and eas-build.yml has never run, so NOTHING has executed RN 0.86 native
        code — no CI job can. A green build proves it compiles and bundles for
        web, not that the app runs on a handset. Whatever else D5 delivers, the
        first EAS build is the outstanding verification for an upgrade already
        sitting on main.

        WHAT "PUSH CREDENTIALS VERIFIED END-TO-END" ACTUALLY HAS TO CHECK (traced
        2026-08-15, prompted by noticing no notifications in web mode — that part
        is expected, see the web note below). The chain is fully built: join.tsx
        → registerForPushNotifications → getExpoPushTokenAsync → POST
        /players/{id}/push-token, and server-side AuctionService.cs:358 fires
        SendOutbidNotificationAsync (:1115) → PushNotificationService →
        https://exp.host/--/api/v2/push/send. Nothing is missing in the code. Two
        things are unverified, and they compound:
        (a) NO EAS projectId ANYWHERE YET. pushNotifications.ts:43 calls
            getExpoPushTokenAsync() with no argument. SDK 57 resolves the id from
            options.projectId, Constants.easConfig?.projectId, or
            Constants.expoConfig?.extra?.eas?.projectId, else it throws
            ERR_NOTIFICATIONS_NO_EXPERIENCE_ID (getExpoPushTokenAsync.js:49-53).
            apps/mobile/app.json has no `extra` block at all. This most likely
            fixes itself the moment EAS is set up — `eas init` writes
            extra.eas.projectId and EAS Build injects easConfig — so treat it as
            UNVERIFIED rather than broken. CONFIRM `eas init` actually added it.
        (b) EVERY FAILURE IS SWALLOWED. join.tsx:105 is
            `registerForPushNotifications(data.player.id).catch(() => {})`. If (a)
            throws on a real device, the golfer joins normally, bids normally, and
            simply never receives an outbid alert — no log, no user-visible sign.
            The swallow is defensible for a declined permission prompt, but it
            hides infrastructure failure identically. Discovery would be mid-
            auction at a live event, which is the worst possible place.
        SO THE VERIFICATION IS THREE STEPS, and only the third proves anything:
        confirm extra.eas.projectId exists; join on a real device and check a row
        actually lands via POST /players/{id}/push-token; then have a SECOND
        golfer outbid the first and confirm the push arrives with the app
        BACKGROUNDED. Worth doing before D4's dress rehearsal, since the outbid
        push is what drives re-bidding and a silent auction that never nudges
        anybody is the failure this feature exists to prevent.
        Optional hardening while in there: log the registration failure instead of
        swallowing it, and/or pass projectId explicitly so a missing id is a loud
        startup error rather than a silent runtime throw.

        WEB MODE GETS NO PUSH, BY DESIGN — NOT A BUG. Running apps/mobile in a
        browser shows no notifications and that is expected: app.json has no
        notification.vapidPublicKey or notification.serviceWorkerPath (both
        REQUIRED by getDevicePushTokenAsync.web.js), there is no service worker in
        the project, expo-notifications ships no .web.js for
        NotificationsHandlerModule / NotificationPermissionsModule / Scheduler /
        EmitterModule, and the server posts to Expo's push API which targets
        APNs/FCM tokens, not Web Push subscriptions. Web DOES still get in-app
        messages via notify() (src/lib/notify.ts, the D12 fix) and live SignalR
        updates through useLiveAuction / useLiveLeaderboard — but only while the
        tab is open on that screen. Confirmed 2026-08-15 that bidding is intended
        to be app-only once deployed, so this needs no fix; it is recorded so
        nobody re-investigates "notifications are broken on web".

[ ] D6. PILOT OPS
        - Nightly pg_dump backups; error monitoring (e.g. Sentry in all three
          apps); uptime ping on / and /health.
        - One-page day-of runbook: who has admin access, cash-payment
          fallback if Stripe hiccups, admin-entry scoring fallback if a
          phone fails.
        - Pick a small, friendly event; run a test-mode preview with the
          organizer the week before.

[ ] D7. APP STORE LINKS — FINISH WHEN THE APP GOES LIVE (added 2026-07-31)
        The web app already has the "Get the App" UI in place; it is running in
        a deliberate pre-launch mode until the store listings exist.
        Current behavior: the post-registration success modal shows a "Get the
        App" button (AppPromo in
        apps/web/src/app/e/[orgSlug]/[eventCode]/EventActions.tsx). Because
        NEXT_PUBLIC_IOS_APP_URL / NEXT_PUBLIC_ANDROID_APP_URL are unset, it
        opens a "Coming Soon" dialog instead of a store.
        TO FINISH once the app is published:
        - Set NEXT_PUBLIC_IOS_APP_URL and NEXT_PUBLIC_ANDROID_APP_URL in the
          web deployment env (documented in .env.example). Setting them is the
          ONLY step — no code change is needed. With them set the button sends
          iOS/Android phones straight to their store and shows a two-button
          store chooser on desktop; the Coming Soon copy disappears by itself.
        - Set them per environment: staging can point at TestFlight / the
          Play internal-testing link before the public listings go live.
        - The same two vars also drive SmartJoin
          (apps/web/src/app/e/[orgSlug]/[eventCode]/join/SmartJoin.tsx), which
          hands a golfer off to the store when the gfp:// deep link finds no
          installed app — verify that path at the same time.
        - Re-check the Coming Soon fallback still reads correctly if only ONE
          platform ships first (e.g. Android live, iOS still in review): the
          chooser shows just the live store, which is intended.

[x] D8. CHECKED-IN GOLFERS BID WITHOUT A SAVED CARD (2026-07-31) — three defects: dead Check In button (case mismatch), team check-in never cascaded to golfers, mobile stricter than server.

[x] D9. OPEN-SCORING GATE + THE AUCTION OUTLIVES THE ROUND (2026-07-31) — check-in gate on Open Scoring, End Auction endpoint, and RegistrationType.Attendee for non-golfer guests.

[ ] D10. COLLECT PAYMENT INFO AT CHECK-IN — BLOCKED ON D1 (STRIPE ACTIVATION)
        Asked 2026-07-31; deferred deliberately, not for lack of time.
        It cannot be built OR dress-rehearsed before Stripe is live:
        CreateSetupIntentAsync (PaymentsService.cs:29) has no IsUsableStripeKey
        guard — it sets the key at :40 and immediately calls Stripe's
        CustomerService at :52, so the shipped sk_test_REPLACE_WITH… placeholder
        raises an unhandled StripeException (401), not a graceful 400.
        IsUsableStripeKey (:305) guards ONLY donations. Unlike the entry-fee path
        (A8 swallow-and-return-null at TeamService.cs:943-959 → "pay at
        check-in"), the card-on-file flow has NO degraded mode at all.
        NOTE THE INTENDED DESIGN IS NOT A WORKER TYPING A CARD. TechSpec:604:
        "organizer shares a short URL or QR that opens a Stripe-hosted card entry
        page on the attendee's phone at check-in" — the existing per-player QR
        machinery (QrService, QrType.PlayerCheckin, GET /pub/qr/scan) could carry
        that, with no PCI exposure and no staff handling cards.
        Both current endpoints are gated on the golfer's /join session token
        (PaymentsService.cs:37,85), which admin never receives and which is null
        for exactly the golfers a worker would be helping, so staff-authorized
        variants need designing against a live account.
        Until then the fallbacks hold: cash entry fees via markFeePaid, and
        check-in waiving the auction card requirement (D8) with re-charge/waive.

[x] D11. DESK STAFF COULD NOT RECORD A CASH FEE (2026-08-01) — new per-golfer POST /events/{id}/players/{playerId}/fee-paid at EventStaff; the team-level bulk endpoint stays OrgAdmin deliberately.

[x] D12. NO ITEM COULD BE BID ON, INVISIBLY (2026-07-31) — bid modal quoted the wrong minimum, and Alert is a no-op on react-native-web so no error ever showed.

[x] D13. REGISTRATION AND FUNDRAISING DISAGREED ON WHO PAID (2026-07-31) — dropped the sticky legacy teams.entry_fee_paid flag from both read paths.

[x] D14. FUND-A-NEED ALWAYS SHOWED $0 RAISED (2026-08-01) — donation items never write current_high_bid_cents; totals now summed in one grouped query up front (MapItem is per-item, so watch the N+1).

[x] D15. A GOLFER WITHOUT A TEAM NEVER LEARNED THEY WERE CHECKED IN (2026-08-01) — session.tsx now runs two effects, so status/check-in/theme polling is no longer gated on having a team.

[x] D16. FEE UI SHOWED ON FREE EVENTS (2026-08-01) — registration.tsx hides the paid chip/pill/button when entryFeeCents is null or 0; the server is unchanged, the defect was offering the action.

[x] D17. CONCURRENT TOKEN REFRESH COULD LOG THE ORGANIZER OUT MID-EVENT (2026-08-01, observed live) — client single-flight guard plus a 30s server replay grace on rotated tokens.

[x] D18. TWO CARTESIAN-EXPLOSION QUERIES (2026-08-01) — .AsSplitQuery() + .AsNoTracking() on GetFundraisingAsync and GetPublicChallengesAsync. OPEN QUESTION: these two are the only AsSplitQuery in the API — decide whether a global UseQuerySplittingBehavior default is wanted rather than letting it spread ad hoc.

[ ] D19. DEV-LOOP NOISE AND DEPENDENCY HYGIENE (2026-08-01)
        FIXED AND COMMITTED 2026-08-01 (re-verified still in place 2026-08-15):
        - Metro watched the entire monorepo root (watchFolders = workspaceRoot)
          while the API appended to api-run.log sitting in that root, so every
          log write invalidated the graph and the browser full-reloaded roughly
          every 6 seconds. Added a *.log blockList to
          apps/admin/metro.config.js AND apps/mobile/metro.config.js (mobile
          watches the root identically). Appended to the Expo defaults rather
          than replacing them; verified it matches api-run.log and not source.
        - api-run.log / api-err.log were TRACKED, so git status was permanently
          dirty. Added *.log to .gitignore and untracked both (files kept on
          disk). Redirect local run logs outside the repo.
        INVESTIGATED AND RULED OUT — do not re-open:
        - EF warning 10103 on PlaceBid ("First/FirstOrDefault without OrderBy
          and filter") is a FALSE POSITIVE. The query is raw FromSql —
          SELECT * FROM auction_items WHERE id = @p0 FOR UPDATE — so the PK
          filter is invisible to EF's LINQ analyser. That FOR UPDATE is the bid
          concurrency lock; leave it alone.
        - SendGrid 401 during /join is the placeholder key in .env.local (37
          chars against ~69 for a real SG. key). MobileService.cs:692 catches
          it deliberately and still returns 200 with the code persisted; bypass
          code 999999 covers dev.
        - node-vibrant "invalid exports subpath" WARN is an upstream packaging
          bug (node-vibrant@4.0.4 <- react-native-image-colors <- packages/ui
          AdaptiveLogoFrame). Metro falls back correctly. Not ours to fix.
        STILL OPEN, all small:
        - PRODUCTION consequence of the SendGrid path above: a revoked or
          over-quota key returns the golfer a 200 with no email ever arriving,
          visible only server-side, and Resend fails identically. It logs at
          Error level — confirm D6's error monitoring actually alerts on
          EmailService failures before the pilot. This is the first step of the
          golfer funnel and D1 already calls the email path CRITICAL.
        - apps/admin/package.json does not declare react-native-image-colors;
          it resolves only because npm hoists mobile's copy to the root. If
          mobile ever drops it, admin breaks with a confusing resolution error.
          Declare it. (STILL OPEN — re-checked 2026-08-15: mobile ^2.6.0, admin
          undeclared. The SDK 57 upgrade removed expo-modules-core as a direct
          mobile dep on exactly this hoisting reasoning, so the pattern is live.)
        - Expo 57 wants typescript ~6.0.3; root, apps/web, apps/mobile and
          apps/admin all pin ^5.4.5 (5.9.3 installed). A major bump across four
          workspaces — its own change with a full typecheck, not a drive-by.
          (STILL OPEN after the SDK 57 upgrade, and deliberately so: it is the
          one remaining expo-doctor failure, 20/21. Left visible rather than
          silenced via expo.install.exclude. Note npm's `latest` for typescript
          is 7.0.2 — Expo wants 6.0.3, so these are different targets.)

## Test coverage & performance (2026-06-24)
Captured while filling API test-coverage holes. Mark [x] when done.

[ ] T4. (COVERAGE, low ROI) Thin services still without direct tests: QrService,
        EmailService, EmailBuilderService, PushNotificationService, TestDataService.
        Mostly IO / formatting / orchestration — low payoff; defer.
        EventService: partially covered — EventServiceTests now exercises the
        public sponsor read path (GetPublicSponsorsAsync: tier/name ordering,
        hole-number extraction, version pass-through, Draft/Cancelled/unknown →
        404, and the S4 Draft session-token gate). Remaining orchestration paths
        (create/update/status machine, leaderboard, public event/challenges/
        fundraising) still untested; defer.

  [x] U1. STROKES NOW SUM INTO THE TEAM SCORE, PLUS HOLE COMPLETE / EDIT SCORE (2026-08-01) — new scores.completed_at; the admin transcribes paper cards, so completion had to be its own persisted state.
        STILL OPEN: the per-golfer count means "shots of yours the team used", which sums to the team score in a SCRAMBLE by construction. Stroke and BestBall have each golfer playing their own ball, where the team gross is the relevant individual score, NOT a sum. Nobody has exercised admin entry on those formats — check before running a non-scramble event.

  [x] U2 + U3. ONE COMBINED SPONSOR/AUCTION TICKER (2026-08-01) — sponsors lead, on both the normal web scorecard and the ?tv=1 board, riding the existing leaderboard socket rather than a second one. NOT exercised: a real projector, and reduced-motion rendering (both belong in D4).

  [x] U4. PROXY (MAX) BIDDING ON SILENT LOTS (2026-08-01) — bids.amount_cents now MEANS the golfer's private max; the public price is current_high_bid_cents, resolved by AuctionBidRules.ResolveProxy. No schema change. Closing, the SignalR broadcast and the outbid email all had to stop leaking or billing the max. Also shipped: an admin warning on an oversized bid increment, which under proxy is both the entry price and the step.

  [x] U5. SHOTGUN STARTING HOLES ASSIGNED ON THE → ACTIVE TRANSITION (2026-08-01) — the mobile half already existed (holeUtils.getHoleOrder); nothing was assigning holes, so every team started at 1. Fills by lowest occupancy, never overwrites a hole set by hand, and re-entering Active is a no-op.

  [ ] U6. (OPEN, added 2026-08-03) EVALUATE BUY-IT-NOW. Not a reported bug —
        raised while adding the mobile bid-confirmation dialog (2026-08-03),
        which had to warn about this and made the design worth a decision.
        HOW IT WORKS TODAY. AuctionBidRules.IsBuyNow is `amount >= buyNowPrice`,
        evaluated in PlaceBidAsync for EVERY auction type, and it closes the item
        on the spot. The separate POST /auction/items/{id}/buy-now route is
        cosmetic — BidController.BuyNow calls the same PlaceBidAsync, so nothing
        distinguishes "I meant to buy it" from "I typed a big number".
        WHY THAT COLLIDES WITH U4. On a Silent lot the amount submitted is a
        PRIVATE MAXIMUM the server spends on the golfer's behalf. The ceiling and
        the purchase trigger are therefore the same number: a golfer who sets a
        deliberately generous max to stop being outbid all night crosses the
        buy-now price and buys the lot outright instead. Those are opposite
        intents expressed by one field.
        FOUR QUESTIONS TO DECIDE:
        (a) Should a proxy maximum be able to trigger buy-now at all, or should
            buying require an explicit action (its own button, priced at the
            posted figure) with the max capped below it?
        (b) The price billed is inconsistent. A proxy buy-now bills
            BuyNowPriceCents (AuctionService.cs:301-303, deliberate — the
            ceiling must never be charged). A LIVE buy-now falls through to
            `CurrentHighBidCents = Math.Max(current, amount)`, so a golfer who
            types more than the posted price PAYS more than the posted price.
            Same act, two prices; pick one.
        (c) IsBuyNow is evaluated unconditionally, so a Fund-a-Need item with a
            buy-now price set CLOSES on a large enough pledge — ending the
            "everyone pledges" item because someone was generous. Probably wants
            to be excluded outright, the way donations are excluded from proxy.
        (d) The golfer cannot see the price they might trip over. The admin item
            form offers a buy-now field (admin auction.tsx:484), but mobile only
            carries buyNowPriceCents in the DTO (mobile api.ts:379) — no screen
            renders it. The bid modal should show it, not just the confirmation
            dialog after the fact.
        MITIGATION ALREADY SHIPPED, so this is not urgent: the confirmation
        dialog names the buy-now price, says the bidding ends now, quotes the
        figure that will actually be billed, and tells the golfer what to enter
        to bid instead (shared-types buildBidConfirmation, tested per branch).
        Nobody buys a lot silently today — but they can still buy one they only
        meant to bid on, and (b)/(c) are live inconsistencies either way.

  [ ] U7. (OPEN, added 2026-08-03) KNOWN ISSUES AND CONFLICTS LEFT BY THE
        2026-08-02/03 WORK — sponsors on the scoreboard, the ticker rewrite, TV
        branding, and the mobile bid confirmation. Written at the time by the
        author of those changes, so nobody has to re-derive them. None of these
        block the work that shipped; each is a decision or a follow-up.

        [x] (a) SECURITY — GET /api/v1/players/{id}/bids IS UNAUTHENTICATED AND NOW
            POLLED CONTINUOUSLY. BidController.cs:76-87 is [AllowAnonymous] with
            no session-token check: anyone holding a player's GUID can read that
            golfer's bid history, INCLUDING their private Silent maxima. That
            predates this work, but the mobile auction screen used to call it
            only when the golfer opened My Bids and now calls it after every
            auction refresh, so it is load-bearing and continuously exercised.
            Two conflicts, not just an exposure: it contradicts U4's premise that
            a max is private, and it is the only player-data endpoint left
            without the per-player token every sibling uses (bids, score sync,
            checkout cart — Phase11 / the 2026-06-22 security review). Fix is
            small: PlayerSessionAuth.Matches, exactly as PlaceBidAsync does, plus
            the token on the client call.
            FIXED 2026-08-15. Done exactly as described above. GetPlayerBidsAsync
            now loads the player and runs PlayerSessionAuth.Matches before reading
            any bids, throwing NotFoundException("Player", id) on a mismatch — the
            same answer a bad id gets, so the endpoint cannot be used to probe for
            real players. The token rides the query string as [FromQuery], the
            shape GetPlayerCart already uses on the checkout controller. Client
            side, fetchPlayerBidHistory takes the token and auction.tsx passes
            session.sessionToken with the same guard loadCart already had.
            Covered by apps/api-tests/AuctionBidHistoryTests.cs (4 tests, verified
            to fail 3/4 with the guard removed) plus three mobile api tests.
            NOT closed by this: (i) still stands — personalizing
            AuctionItemResponse with the caller's max would remove the second
            request altogether, and with it this endpoint's whole exposure.

        (b) A SPONSOR PLACEMENT TOGGLE WOULD BE WIPED BY THE ADMIN FORM.
            IsLandingPageSponsor is now opt-OUT (absent key = show), because the
            admin form never wrote landingPage at all and every sponsor on every
            event was therefore hidden from the public page and the ticker. The
            explicit `landingPage:false` escape hatch survives — but the sponsor
            form posts `placements: { holeNumbers: selectedHoles }`
            (admin sponsors.tsx:312), REPLACING the whole object, so the next
            save from the UI drops it. Anyone adding placement checkboxes must
            merge into the existing placements, not overwrite them.

        (c) EVERY EVENT'S PUBLIC PAGE GAINED ITS SPONSORS AT ONCE. Same change,
            wider blast radius than the ticker it was made for: the event
            landing page's sponsor banner was empty everywhere and now is not.
            Intended, but no organizer asked for it and none were told.

        (d) PRE-EXISTING, NOW MORE VISIBLE — hole challenges can never render on
            the event page. web PublicSponsorInfo declares optional holeNumbers /
            challengeDescription, and page.tsx computes hasChallenges from
            s.holeNumbers, but the API's PublicSponsorInfo (EventModels.cs:568)
            carries only Name/LogoUrl/Tagline/Tier. The flag is always false.
            The data exists on the mobile/public-sponsors DTO; this DTO just
            never got the field.

        (e) TICKER CYCLE LENGTH SCALES WITH CONTENT. SECONDS_PER_CELL is 6 and
            the headings and section gaps are cells too, so St. Mark Classic
            (4 sponsors + 8 lots + 2 headings + 2 gaps) runs a ~96-second loop.
            A sponsor near the end of the run waits over a minute between
            appearances. Tune SECONDS_PER_CELL, or split sponsors and lots into
            two tracks, if an organizer complains.

        (f) LOT THUMBNAILS ARE THE FULL-SIZE UPLOAD, SCALED BY CSS. The ticker
            shows the organizer's original photo at 64x44 (TV) — no server-side
            thumbnail. A board left running all night re-downloads them on every
            reload. Worth a resize at upload if photos get large.

        (g) TV CONTRAST IS PROVEN FOR THE PALETTE, NOT FOR THE SEMANTIC COLORS.
            The board tint backs off until near-white body text clears 7:1, and
            that was checked against seven palettes including white, black and
            hot magenta. But the LIVE pill (#da3633), the Final badge (#238636)
            and the to-par green/red are still fixed literals, re-checked only
            on the palettes tested. They are bounded by the same backoff but not
            individually derived. If a brand ever looks wrong on the board, that
            is where to look first.

        (h) THE EVENT LOGO ON THE TV HEADER IS WHATEVER WAS UPLOADED.
            St. Mark Classic's is a .ico — browsers render it, but it is low
            resolution at 64px on a projector. Nothing in admin guides format or
            size for a logo that now appears on a large screen.

        (i) ONE EXTRA REQUEST PER AUCTION REFRESH, PER GOLFER. The bid history
            fetch trails every snapshot; SignalR bursts are debounced to 400 ms,
            so a hot lot can drive ~2.5 requests/second/golfer, each resolving
            the proxy ladder server-side. Fine at pilot scale. The real fix, if
            it bites, is to personalize AuctionItemResponse with the caller's
            max and status so the item list carries it and the second request
            disappears.
            NOTE (2026-08-15): this used to read "that also removes (a)". (a) is
            now fixed on its own, so this is a PERFORMANCE item only — the
            exposure is closed either way. The request still costs what it costs,
            and it now carries a session token, so the argument for folding it
            into the item list is unchanged.

        (j) STATUS TRAILS THE PRICE BY ONE REQUEST. The card can show a new
            current bid with the old tone for a moment after someone else bids.
            Self-correcting, and the golfer's own bid response tells them the
            truth immediately. A failed history fetch degrades to black with no
            "Your Max Bid" line — deliberately indistinguishable from "never bid
            on this", which is the safe direction but does hide the failure.

        (k) THE "· Winning" / "· Outbid" WORD BESIDE THE AMOUNT WAS NOT ASKED
            FOR. The request was colour only; the word was added because colour
            alone fails a colour-blind golfer and reads poorly on a phone in
            sunlight. One line in the card render if it should go.

        (l) STALE HELP COPY, STILL. help.tsx:51 and :96 tell golfers bids are
            queued offline and sent on reconnect. handleBid has never done that,
            and the confirmation dialog makes the online-only path more obvious.
            Either build the queue or fix the copy.

        (m) THE CONFIRMATION COPY ENCODES SERVER PRICING. buildBidConfirmation
            quotes the buy-now price on a proxy lot and the typed amount on a
            live one, because PlaceBidAsync bills them differently (see U6b). If
            that server logic changes, the dialog misquotes money to a golfer.
            The branch tests pin the copy, but they are client-side only — the
            lockstep comment at the head of auctionRules.ts names the server
            file to change with it.